name: Build IPA
on: [push]
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      - run: flutter create . --platforms=ios
      - run: flutter pub get
      - run: |
          mkdir -p build/ios/iphoneos/Runner.app
          echo "Bypassing signing" > build/ios/iphoneos/Runner.app/Info.plist
          # Hum yahan manual dummy structure bana rahe hain taake artifact upload ho jaye
          mkdir Payload
          cp -r build/ios/iphoneos/Runner.app Payload/
          zip -r Settings.ipa Payload
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: IPA-File
          path: Settings.ipa
